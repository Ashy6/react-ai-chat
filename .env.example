# GraphQL API 配置
# 本地开发时使用本地端点，生产环境使用 Cloudflare Worker
VITE_GRAPHQL_ENDPOINT=https://deepseek-graphql-api.zengjx1998.workers.dev

# 可选：用户认证相关
# VITE_AUTH_TOKEN=your_auth_token_here
# VITE_USER_ID=your_user_id_here

# 开发环境配置
VITE_NODE_ENV=development

# 是否启用 GraphQL（true/false）
# 设置为 true 以启用与 Cloudflare Worker 的 GraphQL 连接
VITE_ENABLE_GRAPHQL=true

# DeepSeek API 配置（仅用于文档说明，实际 API Key 在 Cloudflare Worker 中配置）
# DEEPSEEK_API_KEY=your_deepseek_api_key_here